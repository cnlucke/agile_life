<div class="container">
  <br>
  <h1>Create a New Group</h1>

  <% if flash[:notice] %>
    <div class="notice"><strong style="color: red;"><%= flash[:notice] %></strong></div>
  <% end %>

  <%= form_for @group do |f| %>
    <div class="form-group row">
      <%= f.label 'Group Name:', class: "col-sm-2 col-form-label" %>
      <div class="col-sm-10">
        <%= f.text_field :name, class: "form-control" %>
      </div>
    </div>
    <div class="form-group row">
      <div class="col-sm-2">Members:</div>
      <div class="col-sm-10">
        <div class="form-check">
          <%= f.collection_check_boxes :member_ids, User.all[1...User.all.length], :id, :name %>
        </div>
      </div>
    </div>
    <%= f.submit 'Create Group', class: "btn btn-primary"%>
  <% end %>
</div>
